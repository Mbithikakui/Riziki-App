services:
  - type: web
    name: riziki-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py migrate"
    startCommand: "cd backend && waitress-serve --port=$PORT core.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.1
      - key: DJANGO_SETTINGS_MODULE
        value: core.settings